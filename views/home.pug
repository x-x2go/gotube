extends layouts/main
include mixins/videoBlock

block content
    .home-videos 
        each item in videos
            +videoBlock({
                id: item.id,
                thumbnail: item.thumbnail,
                title: item.title,
                views: item.views,
                videoFile: item.fileUrl
            })
